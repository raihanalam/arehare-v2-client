import DashboardLayout from "../layouts/dashboard/DashboardLayout";
const DashboardPage = () => {
  return <DashboardLayout />
};

DashboardPage.layout = 'dashboard'; // Indicate that this page uses the DashboardLayout

export default DashboardPage;
